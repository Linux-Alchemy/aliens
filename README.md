# aliens

<!-- TODO: write up README properly when project is done -->

<!-- HYPRLAND NOTE (remove this when done):
     Added a window rule to ~/.config/hypr/hyprland.conf to force the game window floating.
     Hyprland tiles it by default. Rule matches class:main.py + title:AlienInvasion.
     REMOVE THE RULE when this project is finished - it's just dev cruft. -->
